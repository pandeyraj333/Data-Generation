# Core async and communication
asyncio  # built-in, no need to install
pydantic

# Langchain + OpenAI support
langchain
langchain-openai

# LangGraph (for ReAct agent)
langgraph

# MCP tooling adapter for LangChain
langchain-mcp-adapters  # assuming pip-installable or custom module

# OpenAI (if using ChatOpenAI model)
openai

# Web server interface (FastMCP)
fastapi
uvicorn
httpx

# Fake data generation
faker

# Utility
python-dotenv
tqdm
typing-extensions
